/**
 * @param {number[]} arr
 * @param {number} target
 * @return {number}
 */
var findBestValue = function(arr, target) {
  arr.sort((a,b)=>a-b)
  let len = arr.length
  let left = 0;
  let right = len - 1
  let temp;
  while(left<right){
    
  }
};