.xButton 
  margin: 2px
  box-sizing: border-box; //mobile border 以内的设置都是width height 传统的只是内容
  display inline-block
  padding 6px 1em
  border-radius 4px
  color #ffffff
  font-family inherit
  background-color #000
  text-align center
  cursor pointer
  user-select: none
  &.primary
    background-color #000099
  &.circle
    border-radius 1.5em
  &.warning
    background-color: yellow 
.ripple
  position relative
  &::after
    content ""
    position absolute
    width 100%
    height 100%
    top 0
    left 0
    display block
    background-image radial-gradient(circle, #fff 10%,transparent 11%)
    background-position 50%
    background-repeat: no-repeat 
    transform scale(12)
    transition transform  .6s, opacity .6s
    opacity 0
    pointer-events: none
  &:active::after
    opacity 1
    transform:  scale(0)
    transition 0s
