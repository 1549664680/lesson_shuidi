1. px(像素)
px单位的名称为像素，它是一个固定大小的单元
一个像素（1px）就是（电脑/手机）屏幕上的一个点
2. em(相对长度)
em单位的名称为相对长度单位，它是用来设置文本的字体尺寸的
em是相对于父级元素的单位，会随父级元素的属性（font-size或其它属性）变化而变化
一般浏览器默认1em=16px
比如父元素设置fonst-size:16px,子元素设置1em,那么子元素的px大小为16px
3. rem(相对根元素)
是相对于根目录（HTML元素）的，所有它会随HTML元素的属性（font-size）变化而变化